#!/bin/sh
set -e

if [ "$1" = "remove" ]; then
    if [ -x "`which systemctl 2>/dev/null`" ]; then
	systemctl stop tvsc-virtual-tower-ui.service
	systemctl disable tvsc-virtual-tower-ui.service
    fi
fi
