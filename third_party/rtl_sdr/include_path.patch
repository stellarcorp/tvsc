--- src/librtlsdr.c	2018-08-26 08:54:51.000000000 -0400
+++ src/librtlsdr.c	2022-11-19 23:05:42.491742176 -0500
@@ -27,7 +27,7 @@
 #define min(a, b) (((a) < (b)) ? (a) : (b))
 #endif
 
-#include <libusb.h>
+#include "libusb-1.0/libusb.h"
 
 /*
  * All libusb callback functions should be marked with the LIBUSB_CALL macro
