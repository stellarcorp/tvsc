syntax = "proto3";

import "nanopb.proto";

package tvsc.comms.radio.nano;

message Packet {
  uint32 protocol = 1;

  uint32 sequence_number = 2;

  uint32 sender = 3;

  bytes payload = 4 [(nanopb).max_size = 62];
}
